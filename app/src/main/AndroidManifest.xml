<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.phrenologue.dreamcatcherapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".Activities.StatsActivity"></activity>
        <activity android:name=".Activities.StatsPackagesActivity" />
        <activity android:name=".Activities.DreamsPackagesActivity" />
        <activity android:name=".Activities.Splash.SplashActivity"></activity>
        <activity android:name=".Activities.Login.SignUp" />
        <activity android:name=".Activities.SleepDreamInputActivity" ></activity>
        <activity android:name=".Activities.ProfileActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.Login.LoginActivity" ></activity>
    </application>

</manifest>